# GXF github workflows
## Run build.yml for testing
To run build.yml for testing add the text cucumber_testing anywhere in your PR title, this will immediately trigger build.yml. 
Afterwards you can remove cucumber_testing again.